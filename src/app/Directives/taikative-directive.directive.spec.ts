import { TaikativeDirectiveDirective } from './taikative-directive.directive';

describe('TaikativeDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new TaikativeDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
